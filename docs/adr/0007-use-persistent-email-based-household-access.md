# Use persistent email-based Household Access

Replace expiring, single-use invitations with Household Owner-managed access keyed by normalized Google email address. An eligible person joins automatically on sign-in, the access binds to their stable Google identity and remains until removed; this deliberately trades recipient choice and link-based delivery for one Members screen and a much smaller access lifecycle, including arbitrary first-match selection when several Households are eligible.
