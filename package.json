{
  "name": "mystuff-firebase-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore,storage --project mystuff-ai-app 'node --test firestore.rules.test.js storage.rules.test.js'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "15.26.0"
  }
}
